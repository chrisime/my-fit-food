https://localhost:443 {
	tls internal

	root * /usr/share/caddy

	@api {
		path /api/*
	}
	handle @api {
		uri strip_prefix /api
		reverse_proxy api:8000
	}

	handle {
		try_files {path} /index.html
		file_server
	}
}
